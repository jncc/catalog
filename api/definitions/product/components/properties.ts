export interface Properties {
    externalId: string
};

export const Schema = {
    "properties": {
        "externalId": {
            "type": "string"
        }
    },
    "required": ["externalId"]
};
